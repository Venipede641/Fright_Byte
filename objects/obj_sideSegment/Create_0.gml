target = global.recentSegment
global.recentSegment = self
targetPosition = [0,0]
//grab target array, and every frame  set target position to oldest element, and destroy oldest element.

//Declare array
//every frame, add a position to the array
positions = []
pathSegment = path_add();

ystatus = "null"
xstatus = "null"

turntimercap = 0
turntimer = turntimercap