/// @description Insert description here
// You can write your code in this editor
if (place_meeting(x, y, obj_player_master)) {
    // Show interaction text when the player is near
    show_text = true;
	if (keyboard_check_pressed(ord("Q")) && obj_player_master.is_holding_item == false && can_dispense)
{
	instance_create_layer(x, y, "Instances", obj_pet_food)
	can_dispense = false
	alarm[0] = game_get_speed(gamespeed_fps) * 10
}

} else {
    // Hide interaction text when the player is not near
    show_text = false;
}